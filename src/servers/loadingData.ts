import { producers } from '../mocks/producers'
import { texts } from '../mocks/texts'
import { top } from '../mocks/top'

export const loadingProducers = () => producers

export const loadingTop = () => top

export const loadingTexts = () => texts